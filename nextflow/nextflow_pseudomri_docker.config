// Docker overlay for the pseudo-MRI anatomy backend.
// The container entrypoint points this at its packaged nextflow.config.
includeConfig "${System.getenv('MEGFLOW_DOCKER_BASE_CONFIG') ?: 'nextflow_for_docker.config'}"

params.megflow.datasets.docker_input.anatomy = [
    method: "pseudomri"
]
